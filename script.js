function Results(){
  var hypotenuse = parseInt(prompt("Enter side digits here"));
  var adjacent = parseInt(prompt("Enter side digits here"));
  var opposite = parseInt(prompt("Enter side digits here"));
}
function Results(){
  var side =[]

}
